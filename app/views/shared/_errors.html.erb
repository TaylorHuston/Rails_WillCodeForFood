<% if object.errors.any? %>
  <div id="error_explanation">
    <strong><%= pluralize(object.errors.count, "error") %> prohibited this record from being saved:</strong>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
